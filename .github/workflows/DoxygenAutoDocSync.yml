name: DoxygenAutoDocSync
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    
    - name: Checkout code with submodules
      uses: actions/checkout@v3
      with:
        submodules: recursive

    - name: Set env
      run: |
        BRANCH_NAME_BUF=$(echo $GITHUB_REF | sed -e 's/.*\///')
        OWNER=$(echo ${{ github.repository }} | cut -d'/' -f1)
        REPO_NAME=$(echo ${{ github.repository }} | cut -d'/' -f2)
        echo "BRANCH_NAME=$BRANCH_NAME_BUF" >> $GITHUB_ENV 
        echo "OWNER=$OWNER" >> $GITHUB_ENV 
        echo "REPO_NAME=$REPO_NAME" >> $GITHUB_ENV 
  
    - name: Prepare Variables
      id: set_output
      run: |
        echo "::set-output name=BRANCH_NAME::${{ env.BRANCH_NAME }}"
        echo "::set-output name=DOC_REPO::${{ env.OWNER }}/DOC-${{ env.REPO_NAME }}"
      
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: artifact
        path: ./
     
  call-reusable-workflow:
    needs: build
    uses: 701Enti/Github-WorkFlows/.github/workflows/doxygen-auto-code-to-doc.yml@master
    with:
      docRepo: ${{ needs.build.steps.set_output.outputs.DOC_REPO }}
      BRANCH_NAME: ${{ needs.build.steps.set_output.outputs.BRANCH_NAME }}
    secrets:
      docRepoSecret: ${{ secrets.DOC_REPO_SYNC_SECRET }}
      
        
